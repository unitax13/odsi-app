<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">

    <title>Notatki</title>
</head>
<body>
<div class="container">
    <h1 class="mb-4"> Memos </h1>
    <a href="/memos/new" class="btn btn-success">New Memo</a>

    <% memos.forEach(memo => { %>
        <div class="card mt-4">
            <div class="card-body">
                <h4 class="card-title"><%= memo.title %></h4>
                <div class="card-subtitle text-muted mb-2">
                    Last opened: <%= memo.createdAt.toLocaleString() %>
                </div>
                <div class="card-text mb-2"><%= memo.description %>
                </div>

                <a href="memos/<%= memo.id %>" class="btn btn-primary">Open</a>
                <a href="memos/edit/<%= memo.id %>" class="btn btn-info">Edit</a>

                <form action="/memos/<%= memo.id %>?_method=DELETE" method="POST" class="d-inline">
                <button type="submit" class="btn btn-danger">Delete

                </button></form>
            </div>
            
        </div>
    <%}) %>
</div>

    
</body>
</html>